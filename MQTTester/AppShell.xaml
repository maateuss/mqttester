<?xml version="1.0" encoding="UTF-8"?>
<Shell xmlns="http://xamarin.com/schemas/2014/forms" xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
       xmlns:views="clr-namespace:MQTTester.Views"
       x:Class="MQTTester.AppShell"
       Shell.NavBarIsVisible="False">
    <Shell.Resources>
        <ResourceDictionary>
            <Style TargetType="ContentPage">
                <Setter Property="Padding">
                    <Setter.Value>
                        <OnPlatform x:TypeArguments="Thickness">
                            <On Platform="iOS" Value="0,20,0,0"/>
                        </OnPlatform>
                    </Setter.Value>
                </Setter>
            </Style>
        </ResourceDictionary>
    </Shell.Resources>
    <TabBar >
            <ShellContent Route="sub"
                          Title="Subscriber"
                          Icon="subscribe.png"
                          ContentTemplate="{DataTemplate views:SubscriberView}"/>
            <ShellContent Route="pub"
                          Title="Publisher"
                          Icon="publish.png"
                          ContentTemplate="{DataTemplate views:PublisherView}"/>
                
    </TabBar>
</Shell>
